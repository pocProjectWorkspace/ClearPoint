{
  "name": "mindssparc-assessment",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "build:api": "pnpm --filter @mindssparc/shared-types build && pnpm --filter @mindssparc/question-bank build && pnpm --filter @mindssparc/diagnostic-rules build && pnpm --filter @mindssparc/api build",
    "start": "cd apps/api && node dist/index.js"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
